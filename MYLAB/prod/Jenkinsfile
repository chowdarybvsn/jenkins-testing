pipeline {
   agent any
   options {
        skipDefaultCheckout(true) // Skip checkout by default
   }
   stages{
       stage("first"){
          when {
                // Check if changes were made to the specified folder
                changeset "MYLAB/prod/**"
          }
          steps{
            script{
                sh 'pwd'
            }
          }
       }
   }
}
