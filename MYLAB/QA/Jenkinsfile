pipeline {
   agent any
   environment {
        
        BRANCH_NAME = "dev"
   }
   stages{
       stage("first"){
            steps{
               script{
                   sh 'git diff --name-only'
                   sh 'uptime'
               }
            }
       }
   }
}

