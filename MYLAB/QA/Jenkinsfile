pipeline {
   agent any
   
   stages{
       stage("first"){
         when { changeset "/terraform/**"}
            steps{
               script{
                   sh 'echo "QA env"'
                   sh 'uptime'
               }
            }
       }
   }
}

