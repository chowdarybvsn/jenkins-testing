pipeline {
   agent any
   environment {
        
        BRANCH_NAME = "dev"
   }
   stages{
       stage("first"){
            steps{
               script{
                   sh 'git diff  origin/master --name-only'
                   sh 'uptime'
               }
            }
       }
   }
}

